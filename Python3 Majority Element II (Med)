class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        s = set(nums)
        res = []
        for x in s:
            if nums.count(x) > len(nums)/3:
                res.append(x)
        return res
