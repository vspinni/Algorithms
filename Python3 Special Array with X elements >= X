import bisect

class Solution:
    def specialArray(self, nums: List[int]) -> int:
        special = -1
        nums.sort()
        for x in range(1,len(nums)+1):
            if len(nums) - bisect.bisect_left(nums,x) == x:
                special = x
        
        return special
