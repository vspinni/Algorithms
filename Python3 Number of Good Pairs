class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        count = 0
        for i,x in enumerate(nums):
            for j,y in enumerate(nums):
                if x == y and i < j:
                    count +=1
        return count
        
